package aws.boot.user.repository;

public class UserJPARepositoryCustomImpl implements UserJPARepositoryCustom{
	/**
	private final EntityManager entityManager;

	@Autowired
	public UserJPARepositoryCustomImpl(JpaContext context) {
		this.entityManager = context.getEntityManagerByManagedType(UserDomain.class);
	}**/
}
